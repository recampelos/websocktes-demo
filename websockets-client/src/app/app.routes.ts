import { Routes } from '@angular/router';
import { WebSocketsChatComponent } from './src/app/chat/web-sockets-chat/web-sockets-chat.component';

export const routes: Routes = [
  { path: '', component: WebSocketsChatComponent },
];
